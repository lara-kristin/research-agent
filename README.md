# Research Agent

An LLM-powered multi-agent system that turns a research question into a
structured, source-traceable evidence brief.

MSc Artificial Intelligence, University of Essex Online.

httpbin.org is used to produce deterministic HTTP error responses when testing retry behaviour. It is not used at runtime.